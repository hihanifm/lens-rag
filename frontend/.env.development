# Dev only — points directly at the local FastAPI container.
# In production builds (npm run build) this file is NOT used;
# the API URL is derived from BASE_URL (set via VITE_BASE_PATH at build time).
VITE_API_URL=http://localhost:37000
